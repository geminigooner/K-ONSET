{
  "name": "k-onset-artifact-desk",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "build": "echo 'No build step needed'",
    "check": "node --check app.js && node --check server.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "dotenv": "^17.2.3"
  },
  "engines": { "node": ">=20" }
}
